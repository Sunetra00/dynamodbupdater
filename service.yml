kind: Service
apiVersion: null
metadata:
  name: dynamodbupdater
  namespace: eksapp
spec:
  type: NodePort
  ports:
    port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: dynamodbupdater
